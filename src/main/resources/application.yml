spring:
  redis:
    host: localhost
    port: 6379

  datasource:
    url: jdbc:mysql://localhost:3306/demo
    username: root
    password: 123salou

  jpa:
    show-sql: true

mybatis:
  mapper-locations: classpath*:mapper/*Mapper.xml

server:
  port: 8098